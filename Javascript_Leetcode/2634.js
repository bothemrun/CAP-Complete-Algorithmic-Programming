//m0
//builtin Array.filter(function(cur_value, cur_index, arr))

/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
var filter = function(arr, fn) {
    return arr.filter(fn);
};
